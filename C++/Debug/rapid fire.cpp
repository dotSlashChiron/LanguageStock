#include <iostream>
using namespace std;
void *vp();
enum three{
    one,two,three
};
    // enum (green,yellow,red); Enums don't take any sorta parameters
int homain(){
    // long float x; Invalid Type of Combination
    // char *cp = vp; a value of type "void *(*)()" cannot be used to initialize an entity of type "char *"
    int code = three;
    // int *p = new | after new there is no data_type mentioned 
    // int const *p = total; Identifier total is undefined
    // const int array_size; Throws error becuase const needs a initialized value at the time of declaration
    // for(int i;i<10;i++)cout << i << "\n"; Throws error because there is no body;
    // int & number = 100; Throws error because initial value must be a non-const valu
    // float *p = new int[10]; Throws error because in the first case it is float and in the second case it is int
    // int public =1000; No identifier public is a keyword
    // char name[3] = "USA";a value of type "const char [4]" cannot be used to initialize an entity of type "char [3]"

return 0;
}