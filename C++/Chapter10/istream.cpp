#include <iostream>
int main(){
    // char c;
    // std::cin.get(c);
    // while (std::cin.get(c)){
    //     std::cout << c;
    // }
    std::cout.put(11); // converts the integer value into char and puts the char which has the ascii value equal to 11
}