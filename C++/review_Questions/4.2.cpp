#include <iostream>
using namespace std;
int display(...){
    return 0;
}
int rudradevmain(){
//  float average(a,b);--Error because data-type needs to be declared
    // int mul(int a,b); --Erroor because the last parameter does not have any type
    int display(...); // No error
    // void Vect(int? &v,int &size); ? should be removed
    // print(float Data[],size=20);// --Error because size has no type

return 0;
}